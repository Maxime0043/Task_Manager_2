import express from "express";

const router = express.Router();

// Import Middlewares

// Import Routes

// Import Controllers

// Define the API routes

// Export the router
export default router;
